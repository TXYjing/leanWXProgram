<!--index.wxml-->
<!--1. 小程序的数据绑定：{{}}->mustuche语法 -->
<view>{{name}}</view>
<!-- 2.列表展示：wx:for="{{参数}}" -->
<view wx:for="{{students}}" wx:key="{{index}}">{{item.name}}---{{item.age}}</view>
<!-- 3.事件监听 -->
<view>{{counter}}</view>
<button size="mini" bindtap="handleCount">+</button>
<view class="container">
<view>知识点1：</view>
</view>
